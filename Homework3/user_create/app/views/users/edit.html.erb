<%= form_for :user, url: user_path(@user), method: :patch do |f| %>
  <p>
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </p>
  <p>
    <%= f.submit %>
  </p>
  
  <%= link_to 'Back', users_path %>
<% end %>