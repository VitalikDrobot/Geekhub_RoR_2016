<h1>Pets:</h1>
<%= link_to 'Add new name pet', new_pet_path %>
<% @pets.each do |pet| %>
	<p>Name: <%= pet.name %> </p>
	<%= link_to 'Show', pet %>
	<%= link_to 'Edit', edit_pet_path(pet) %>
	<%= link_to 'Delete', pet_path(pet), method: :delete %>
<% end %>
